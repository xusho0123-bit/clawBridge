# ============================================================
#  Antigravity Bridge v2.3 — Configuration
#  設定檔範本 / Configuration Template
#
#  Copy this file to .env and fill in your values:
#  複製此檔案為 .env 並填入你的值：
#    cp .env.example .env
#
#  Or use the setup wizard / 或使用設定精靈:
#    npm run setup
# ============================================================

# --- Required 必填 ---

# Telegram Bot Token (from @BotFather)
# Telegram Bot Token（來自 @BotFather）
# Steps 步驟:
#   1. Open Telegram → search @BotFather / 開啟 TG → 搜尋 @BotFather
#   2. Send /newbot → follow instructions / 傳 /newbot → 按指示操作
#   3. Copy the token here / 複製 token 到此
TELEGRAM_BOT_TOKEN=

# --- Optional 選填 ---

# Your Telegram User ID (restricts bot to you only)
# 你的 Telegram User ID（限制只有你能使用 Bot）
# Get it: send any message to @userinfobot / 取得方式：傳訊息給 @userinfobot
# ALLOWED_USER_ID=

# Project folder for Antigravity IDE to open (triggers Language Server)
# Antigravity IDE 要開啟的專案資料夾（觸發 Language Server 啟動）
# PROJECT_PATH=/path/to/your/project

# AI Model ID (see Antigravity settings for available models)
# AI 模型 ID（可用模型請看 Antigravity 設定）
# AI_MODEL=MODEL_PLACEHOLDER_M18

# Response timeout in ms (default: 120000 = 2 min)
# AI 回應超時時間（預設 120000 = 2 分鐘）
# POLL_TIMEOUT_MS=120000

# Whisper STT model (for voice message transcription)
# Whisper 語音辨識模型（語音訊息轉文字用）
# Options 選項: turbo (fast/快), base, small, medium, large (accurate/精確)
# WHISPER_MODEL=turbo

# --- System Prompt 系統提示詞 ---
# Injected at the start of each conversation. Defines AI capabilities.
# 每個新對話開始時注入，定義 AI 的能力。
# Customize as needed. Use \n for newlines.
# 可自行修改。用 \n 換行。

SYSTEM_PROMPT=你是使用者的 AI 助手，透過 Telegram 對話。規則：\n1. 直接回答問題，不要自我介紹、不要列出你的能力清單\n2. 不要主動分析工作目錄的檔案，除非使用者要求\n3. 回覆簡潔有用，像朋友聊天一樣自然\n\n記憶功能（內部使用，不要向使用者說明）：\n- 記住資訊：回覆中加 REMEMBER: [標籤] 內容\n- 查歷史：回覆中加 RECALL: 關鍵字\n- [記憶] 區塊的資訊請直接參考，不需複述\n\n媒體回覆格式：MEDIA: /absolute/path/to/file\n語音回覆額外加：asVoice: true
